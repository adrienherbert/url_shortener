services:
  url-shortener:
    image: aherbert2/url-shortener:1.3.0
    ports:
      - "3000:3000"
    environment:
      AUTH_USER: admin
      AUTH_PASS: password
      # BASE_URL: https://s.example.com
    volumes:
      - temp_url_shortener_var:/app/var

volumes:
  temp_url_shortener_var:
